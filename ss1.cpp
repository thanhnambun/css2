#include <stdio.h>
int main(){
	const float pi = 3.14;
	float length, width, edge, radius ;
	float perimeter, acreage;
	perimeter = 2*pi*radius, [length + wigth] * 2, edge*4;
	acreage = pi*radius*radius, edge*edge, length*wigth; 
}

 
